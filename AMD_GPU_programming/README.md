Run `source setup.sh` in terminal to setup the dev env for the first time or run an incremental build

Run `source setup.sh --reset` to do a regenerate the build files

Run `source setup.sh --set-path` to set teh path to the llvm builds
