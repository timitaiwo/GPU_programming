
This directory defines the IaC to provision a GPU for CUDA devlopment. runpod.ai was chosen as the gpu provider.

copy and rename .secrets.auto.tfvars.sample to .secrets.auto.tfvars.. This is where the runpod api key would be stored and must not be committed to the repository.
